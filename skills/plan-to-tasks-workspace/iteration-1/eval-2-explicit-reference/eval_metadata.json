{
  "eval_id": 2,
  "eval_name": "explicit-reference",
  "prompt": "create tasks from that plan",
  "assertions": [
    "A preview table is shown before any task files are written",
    "Exactly 4 task specs are produced, one per plan step",
    "Each task spec contains all required create-task sections",
    "Step 2 depends on Step 1",
    "Step 3 depends on Steps 1 and 2",
    "Step 4 depends on Steps 1, 2, and 3",
    "Dependency lines appear in both markdown body and frontmatter depends_on field"
  ]
}
